import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.fit.federtennis',
  appName: 'auth-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
